<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 선택된 상태 -->
    <item android:state_checked="true" android:drawable="@drawable/bg_tab_white"/>
    <!-- 기본(선택 안 됨) -->
    <item android:drawable="@drawable/bg_tab_gray"/>
</selector>
